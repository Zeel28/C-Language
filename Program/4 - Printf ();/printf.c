#include<stdio.h>
int main()
{
	// Displays the string inside quotations
    printf("printf() is predefined function. \n");
    printf("printf and scanf is NOT a KEYWORD.\n");
    printf("Format :- (\"__________ Enscape sequere\");\n\n");
    printf("Note :- printf() is one of the main output function. The function sends formatted output to the screen");
    
	return 0;
}
